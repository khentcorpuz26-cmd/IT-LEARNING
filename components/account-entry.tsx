import {Code2, ArrowLeft, Mail, ShieldCheck} from 'lucide-react';

export default function AccountEntry({mode}:{mode:'login'|'signup'}) {
  const signup=mode==='signup';
  return <div className="app"><header className="topbar"><a href="/" className="logo"><span><Code2 size={22}/></span>devshelf<span className="logo-dot">.</span></a><nav aria-label="Account navigation"><a href="/">Learning library</a><a href={signup?'/login':'/signup'}>{signup?'Log in':'Sign up'}</a></nav></header><main className="account-main"><a className="account-back" href="/"><ArrowLeft size={16}/> Back to learning</a><section className="account-card" aria-labelledby="account-title"><span className="account-icon"><Mail size={26}/></span><p className="eyebrow">YOUR DEVSHELF ACCOUNT</p><h1 id="account-title">{signup?'Create your account':'Welcome back'}<span>.</span></h1><p className="account-description">{signup?'Start your DevShelf account with your Gmail address.':'Continue to your DevShelf account with your Gmail address.'}</p><div className="account-notice" role="status"><ShieldCheck size={20}/><div><strong>Email sign-in is being prepared</strong><p>Account registration and verification emails are not available yet. You can continue using the learning library and tools.</p></div></div><form aria-describedby="account-availability"><label htmlFor="account-email">Gmail address</label><input id="account-email" type="email" placeholder="you@gmail.com" autoComplete="email" disabled/><button className="primary" type="button" disabled>{signup?'Create account':'Send login code'}</button><p id="account-availability" className="account-help">This form will open when email verification is ready.</p></form><p className="account-switch">{signup?'Already have an account?':'New to DevShelf?'} <a href={signup?'/login':'/signup'}>{signup?'Log in':'Sign up'}</a></p></section><p className="account-footnote">Your existing learning progress stays in this browser.</p></main></div>;
}
