'use client';
import {Select,SelectTrigger,SelectValue,SelectContent,SelectItem} from '@/components/ui/select';
export default function LessonJump({sections}:{sections:{id:string,title:string}[]}){return <div className="lesson-jump"><label>Jump to a section</label><Select defaultValue="outcomes" onValueChange={v=>{if(v){const target=document.getElementById(v);target?.scrollIntoView({behavior:window.matchMedia('(prefers-reduced-motion: reduce)').matches?'instant':'smooth'});target?.setAttribute('tabindex','-1');target?.focus({preventScroll:true});history.replaceState(null,'','#'+v)}}}><SelectTrigger aria-label="Jump to a lesson section"><SelectValue/></SelectTrigger><SelectContent>{[{id:'outcomes',title:'What you will learn'},...sections,{id:'quiz',title:'Quick quiz'},{id:'practice',title:'Practice exercises'},{id:'answers',title:'Suggested answers'},{id:'glossary',title:'Glossary'},{id:'references',title:'Further reading'}].map(s=><SelectItem key={s.id} value={s.id}>{s.title}</SelectItem>)}</SelectContent></Select></div>}

