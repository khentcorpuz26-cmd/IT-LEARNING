'use client';
import {useState} from 'react';
import {Code2,Braces,Binary,Link as LinkIcon,Fingerprint,ArrowUpRight,ArrowRight,Copy,Check,Search,Terminal,BookOpen,ShieldCheck} from 'lucide-react';
import {Tabs,TabsList,TabsTrigger} from '@/components/ui/tabs';
import {transform} from '@/lib/toolbox';
import {guides,readingTime} from '@/lib/guides';
const tools=[{id:'json',name:'JSON formatter',icon:Braces,description:'Format, validate, and minify JSON.',tag:'DATA'},{id:'base64',name:'Base64 converter',icon:Binary,description:'Encode and decode UTF-8 text.',tag:'ENCODING'},{id:'url',name:'URL encoder',icon:LinkIcon,description:'Encode or decode URL components.',tag:'WEB'},{id:'uuid',name:'UUID generator',icon:Fingerprint,description:'Generate a random version 4 UUID.',tag:'UTILITY'}];
const resources=guides.map((g,i)=>({name:g.title,label:String(i+1).padStart(2,'0'),category:g.category,description:g.summary,url:'/learn/'+g.slug,color:['orange','cyan','green','blue'][i],minutes:readingTime(g)}));
export default function Home(){const [active,setActive]=useState('json'),[input,setInput]=useState('{"project":"DevShelf","ready":true,"stack":["TypeScript","React"]}'),[output,setOutput]=useState(''),[error,setError]=useState(''),[copied,setCopied]=useState(false),[query,setQuery]=useState(''),[category,setCategory]=useState('All');const selected=tools.find(t=>t.id===active)!;
function run(action:string){setError('');setCopied(false);try{setOutput(active==='uuid'?crypto.randomUUID():transform(active,input,action))}catch(e){setOutput('');setError(e instanceof Error?e.message:'Unable to convert this input.')}}
function select(id:string){setActive(id);setInput(id==='json'?'{"project":"DevShelf","ready":true}':'');setOutput('');setError('');setCopied(false)}
async function copy(){try{await navigator.clipboard.writeText(output);setCopied(true)}catch{setError('Clipboard unavailable. Select and copy the output manually.')}}
const filtered=resources.filter(r=>(category==='All'||r.category===category)&&`${r.name} ${r.description}`.toLowerCase().includes(query.toLowerCase()));
return <div className="app"><header className="topbar"><a href="/" className="logo"><span><Code2 size={22}/></span>devshelf<span className="logo-dot">.</span></a><nav aria-label="Main navigation"><a href="#tools">Tools</a><a href="#resources">Learn</a></nav><div className="top-note"><span/> YOUR EVERYDAY DEV KIT</div></header><main><div className="intro"><div><p className="eyebrow">LESS TAB HOPPING. MORE BUILDING.</p><h1>A good day to <span>build something.</span></h1><p className="subtitle">Practical IT tools and in-depth coding lessons, all in one place.</p></div><div className="terminal-mark"><Terminal size={24}/><span>~/your-workspace<span className="cursor">_</span></span></div></div><section id="tools" aria-labelledby="tools-title"><div className="section-title"><h2 id="tools-title"><Terminal size={19}/>The toolbox</h2><span>04 TOOLS · RUNS IN YOUR BROWSER</span></div><div className="tool-grid">{tools.map(t=><button key={t.id} className={`tool-card ${active===t.id?'selected':''}`} onClick={()=>select(t.id)} aria-pressed={active===t.id}><div><span className="tool-icon"><t.icon size={23}/></span><span className="tool-tag">{t.tag}</span></div><h3>{t.name}<ArrowUpRight size={17}/></h3><p>{t.description}</p></button>)}</div><div className="workbench"><div className="bench-header"><div><span className="file-dot"/><strong>{selected.name}</strong><span className="bench-sub">/ workspace</span></div><span><ShieldCheck size={14}/>Processed locally</span></div>{active!=='uuid'?<div className="editors"><div className="editor"><label htmlFor="input">INPUT <span>{input.length} characters</span></label><textarea id="input" spellCheck={false} value={input} onChange={e=>{setInput(e.target.value);setOutput('');setError('');setCopied(false)}} placeholder={active==='json'?'Paste your JSON here…':'Enter text to encode or decode…'}/></div><div className="editor"><label htmlFor="output">OUTPUT <button onClick={copy} disabled={!output} aria-label="Copy output">{copied?<Check size={15}/>:<Copy size={15}/>} {copied?'Copied':'Copy'}</button></label><textarea id="output" spellCheck={false} readOnly value={output} placeholder="Your result will appear here."/></div></div>:<div className="uuid-area"><Fingerprint size={30}/><h3>A fresh identifier for your next idea.</h3><input aria-label="Generated UUID" readOnly value={output} placeholder="Click Generate UUID to begin"/><button className="quiet" onClick={copy} disabled={!output}>{copied?<Check size={16}/>:<Copy size={16}/>} {copied?'Copied':'Copy UUID'}</button></div>}<div className="bench-actions"><div><button className="primary" onClick={()=>run('encode')}>{active==='json'?'Format JSON':active==='uuid'?'Generate UUID':'Encode'}<ArrowRight size={16}/></button>{active!=='uuid'&&<button className="secondary" onClick={()=>run(active==='json'?'minify':'decode')}>{active==='json'?'Minify':'Decode'}</button>}<button className="quiet" onClick={()=>{setInput('');setOutput('');setError('');setCopied(false)}}>Clear</button></div><span>No uploads. No account required for these tools.</span></div>{error&&<p className="error" role="alert">{error}</p>}</div></section><section id="resources" aria-labelledby="resources-title"><div className="section-title resources-title"><div><h2 id="resources-title"><BookOpen size={19}/>Your learning library</h2><p>Read, practice, and build your skills. Complete lessons, right here.</p></div><label className="search"><Search size={17}/><input aria-label="Search lessons" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Find a lesson…"/></label></div><Tabs value={category} onValueChange={v=>setCategory(String(v))}><TabsList className="filters">{['All','Web foundations','Programming','Developer workflow','Backend & APIs'].map(c=><TabsTrigger key={c} value={c}>{c}</TabsTrigger>)}</TabsList></Tabs><div className="resource-grid">{filtered.map(r=><a key={r.name} className="resource-card" href={r.url}><div className="resource-top"><span className={`resource-icon ${r.color}`}>{r.label}</span><ArrowUpRight size={18}/></div><h3>{r.name}</h3><p>{r.description}</p><div className="resource-meta"><span>{r.category}</span><span>{r.minutes} MIN READ</span></div></a>)}</div>{!filtered.length&&<div className="no-results">No lessons match your search. Try another term or category.</div>}</section><footer><span><Code2 size={16}/>devshelf.</span><p>Keep your tools close. Keep learning.</p><a href="#tools">Back to tools ↑</a></footer></main></div>}

